#include <stdio.h>

int main (int argc, char *argv[])
{


	//child process 5
	if (pid[4] == 0)
	{
		//close read pipe



		//write to pipe

	}

}

char* getInput ()
{
	char buf[128];
	printf("Enter a Message: ");
	char *input = fgets(buf, 128, stdin);
	getchar();
	return input;
}
